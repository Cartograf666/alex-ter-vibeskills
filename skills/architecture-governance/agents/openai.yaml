interface:
  display_name: "Architecture Governance"
  short_description: "Enforce project layers and change boundaries"
  default_prompt: "Use $architecture-governance to bootstrap or enforce the project architecture manifest and cross-layer approval policy."
