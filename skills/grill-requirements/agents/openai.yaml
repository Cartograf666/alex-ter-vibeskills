interface:
  display_name: "Interview Requirements"
  short_description: "Resolve product and technical decisions one by one"
  default_prompt: "Use $grill-requirements to inspect available context, then interview me one question at a time until every material decision is resolved or explicitly open."
